; Command to process files during real time operations
nika_filing

type Q : a single Q or q then enter to interrupt the program then retall

; For testing purpose (before the run)
nika_filing, /noexe, /verb

; /noexe means no execution of ssh, ftp and so forth: just read the data
; /verb if you want a verbose mode
; /noftp if no ftp is requested down to Grenoble share
Control-C Control-C  ; to get out
